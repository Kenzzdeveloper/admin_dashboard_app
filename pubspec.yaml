name: study_club_app
description: Study Club Admin Dashboard
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  http: ^1.2.2
  dio: ^5.0.3
  pretty_dio_logger: ^1.2.0
  intl: ^0.20.2
  provider: ^6.1.2
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  device_preview: ^1.3.1
  shared_preferences: ^2.5.5
  image_picker: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true